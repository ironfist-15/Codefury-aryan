/*package com.codefury.codefury2.controller;

import lombok.Getter;
import lombok.Setter;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;


@RestController
@RequestMapping("/api")
public class LocationController {
    @PostMapping("/location")
    public void handleLocation(@RequestBody location location) {
        // Process the location data
        System.out.println("Received location: Latitude = " + location.getLatitude() + ", Longitude = " + location.getLongitude());
        // Optionally, save the location to a database
    }

    @Setter
    @Getter
    public static class location {

        private double latitude;
        private double longitude;

    }
}
*/

